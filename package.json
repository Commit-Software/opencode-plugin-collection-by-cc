{
  "name": "opencode-plugins-cc",
  "version": "1.0.0",
  "description": "A collection of OpenCode plugins and slash commands",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "index.ts",
    "tsconfig.json"
  ],
  "scripts": {
    "build": "bun build index.ts --outdir dist --target node && tsc --emitDeclarationOnly --declaration --declarationDir dist",
    "prepare": "bun run build",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^0.15.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.1"
  },
  "keywords": [
    "opencode",
    "plugin",
    "code-simplifier",
    "refactoring",
    "code-quality",
    "slash-commands"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/commit-software/opencode-plugin-collection-by-cc"
  }
}
